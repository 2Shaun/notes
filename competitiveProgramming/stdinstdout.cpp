#include <bits/stdc++.h>

using namespace std;

int main() {
	int a, b;
	string x;
	
	cin >> a >> b >> x;
	// \n works faster than endl because endl causes a flush operation
	cout << a << " "<< b << " " << x << "\n";
}
